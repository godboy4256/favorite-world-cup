import Button from "@/components/inputs/button/button";
import React from "react";

const LoginPage = () => {
  return (
    <div>
      <Button text="로그인" />
    </div>
  );
};

export default LoginPage;
